{
  "name": "deltabot",
  "version": "1.0.0",
  "description": "A discord bot that assigns trust values to each user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AlphaRW",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^11.5.1",
    "moment": "^2.24.0"
  }
}
